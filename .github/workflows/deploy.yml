name: Deploy to Bluehost (DISABLED)

on:
  workflow_dispatch:
      # Disabled - use deploy-frontend.yml for frontend deploys
          # Backend is auto-deployed by Railway from GitHub

          jobs:
            noop:
                runs-on: ubuntu-latest
                    if: false
                        steps:
                              - run: echo disabled
                              
