name: Deploy to Bluehost (DISABLED)

# Disabled - frontend deploys use deploy-frontend.yml.
# Backend is auto-deployed by Railway from GitHub.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo disabled
